{
  "enable": {
    "description": "是否启用恶意消息检测",
    "type": "bool",
    "default": true,
    "hint": "关闭后插件不再对任何消息进行检测。"
  },
  "provider_id": {
    "description": "用于检测/生成警告的模型提供商",
    "type": "string",
    "default": "",
    "hint": "留空则自动使用当前会话配置的聊天模型提供商。建议选用响应快、成本低的模型（如各厂商的 flash / mini 系列）以降低延迟与开销。",
    "_special": "select_provider"
  },
  "scan_private": {
    "description": "是否检测私聊消息",
    "type": "bool",
    "default": true,
    "hint": "私聊触发警告时只累计 x，不尝试禁言。"
  },
  "scan_group": {
    "description": "是否检测群聊消息",
    "type": "bool",
    "default": true
  },
  "scan_command": {
    "description": "是否检测指令消息（以指令前缀开头的消息）",
    "type": "bool",
    "default": false,
    "hint": "默认不检测指令，避免对 /help 等指令产生额外开销。"
  },
  "command_prefixes": {
    "description": "指令前缀列表",
    "type": "list",
    "default": ["/"],
    "hint": "当“检测指令消息”关闭时，以这些前缀开头的消息会被跳过。如果你修改过唤醒前缀，请同步修改此处。"
  },
  "min_length": {
    "description": "最小检测长度",
    "type": "int",
    "default": 2,
    "hint": "短于此长度的纯文本消息不检测，以节省开销。建议 2-6。"
  },
  "max_length": {
    "description": "单条消息最大检测长度",
    "type": "int",
    "default": 500,
    "hint": "超过此长度的消息会被截断后再送检，避免消耗过多 token。"
  },
  "stop_event": {
    "description": "检测到恶意消息时拦截事件传播",
    "type": "bool",
    "default": true,
    "hint": "开启后，恶意消息将被拦截，机器人不会再对该消息做出后续回应（如 LLM 回复）。关闭则仅发送警告，不拦截。"
  },
  "warn_at_sender": {
    "description": "警告时 @ 发送者",
    "type": "bool",
    "default": true,
    "hint": "部分平台若不支持 @ 则自动回退为纯文本警告。"
  },
  "warn_message": {
    "description": "模板警告文案（LLM 不可用时回退使用）",
    "type": "text",
    "default": "⚠️ {sender}，你发送的消息被判定为含有严重恶意内容（辱骂/人身攻击/骚扰/威胁/诈骗等），请遵守群规，文明发言。多次违规可能被禁言或移出。当前累计警告次数：{x}。",
    "hint": "正常情况下警告文案由 LLM 以当前人格语气生成，本模板仅作兜底。支持占位符 {sender}、{x}、{reason}。"
  },
  "cooldown": {
    "description": "同一用户警告冷却时间（秒）",
    "type": "int",
    "default": 0,
    "hint": "避免对同一用户的连续消息重复警告。默认 0（不限制，每次恶意都计入 x）。如需抑制刷屏可设为 10-60。"
  },
  "enable_mute": {
    "description": "是否启用自动禁言",
    "type": "bool",
    "default": true,
    "hint": "仅对 aiocqhttp（OneBot 协议端）群聊生效，且要求机器人为群管理员。私聊或机器人非管理员的群不会尝试禁言。"
  },
  "mute_threshold": {
    "description": "触发禁言的累计阈值 x",
    "type": "int",
    "default": 5,
    "hint": "当某用户的警告次数 x 超过此值时触发禁言。例如 5 表示第 6 次警告开始禁言。"
  },
  "mute_multiplier": {
    "description": "禁言时长倍数（分钟）",
    "type": "int",
    "default": 10,
    "hint": "禁言分钟数 = 倍数 × 当前 x。例如 x=6、倍数=10 → 禁言 60 分钟。"
  },
  "mute_max_minutes": {
    "description": "单次禁言上限（分钟）",
    "type": "int",
    "default": 43200,
    "hint": "防止 x 过大导致超长禁言。默认 43200 分钟（30 天，OneBot 协议上限）。"
  },
  "whitelist_users": {
    "description": "用户白名单",
    "type": "list",
    "default": [],
    "hint": "不检测这些用户 ID 的消息，也不计入 x。可用于豁免管理员或信任用户。"
  },
  "whitelist_groups": {
    "description": "群聊白名单",
    "type": "list",
    "default": [],
    "hint": "不检测这些群 ID 的消息。"
  },
  "judge_prompt": {
    "description": "判定恶意消息的系统提示词",
    "type": "text",
    "default": "你是一个内容安全审核助手。请判断用户发送的消息是否含有【严重】恶意内容。\n\n判定为严重恶意的标准（满足任一即可）：\n1. 严重辱骂、人身攻击、侮辱性、贬损性言语\n2. 骚扰、恐吓、威胁他人人身或财产安全\n3. 诈骗、诱导转账汇款、钓鱼链接、虚假中奖\n4. 煽动仇恨、歧视、暴力、自残自杀\n5. 散布违法信息（黄赌毒、暴恐、违禁品交易等）\n\n以下情况【不】判定为严重恶意：\n- 普通调侃、玩笑、轻微吐槽、阴阳怪气\n- 正常讨论、争论、表达不满或不同意见\n- 单独的表情符号、语气词\n- 不带恶意的网络用语\n\n请严格只输出如下 JSON，不要输出任何其他文字或解释：\n{\"malicious\": true, \"reason\": \"简要原因，不超过30字\"}\n或\n{\"malicious\": false, \"reason\": \"无\"}",
    "hint": "可自定义判定标准。模型必须输出 {\"malicious\": bool, \"reason\": str} 格式的 JSON。"
  }
}
